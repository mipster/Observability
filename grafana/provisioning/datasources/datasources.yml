apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: Node Exporter
    type: prometheus
    access: proxy
    url: http://host.docker.internal:9100
    editable: true
